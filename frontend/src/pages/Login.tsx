import React, { useState, useEffect } from 'react';

const Login: React.FC = () => {
    return (
        <div>
          {/* Aquí puedes colocar el contenido de tu componente Login */}
          <h1>Iniciar sesión</h1>
        </div>
    );
};

export default Login;